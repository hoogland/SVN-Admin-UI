/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ExternalClassFilterPipe } from './external-class-filter.pipe';

describe('ExternalClassFilterPipe', () => {
  it('create an instance', () => {
    let pipe = new ExternalClassFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
